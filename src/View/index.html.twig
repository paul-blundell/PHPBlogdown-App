{% extends '_extends/master.html.twig' %}

{% block page_content %}

  <h1>PHPBlogdown</h1>
  
  <p>Thank you for downloading the PHPBlogdown application, this is a ready to use website built on Slim framework that uses PHPBlogdown.</p>
  <p>Your categories are listed above and these are being generated by the folders in the <code>posts</code> directory. Click on one of the categories and any markdown documents <code>.md</code> will be listed. Click on a post to see your markdown file converted to HTML.</p>
  <p>This application uses Twig for its templates and the contents of this page can be found in <code>src/View/index.html.twig</code></p>
  <p>Enjoy.</p>

{% endblock %}